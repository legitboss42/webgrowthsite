import { cookies } from "next/headers"; import { redirect } from "next/navigation"; import { readSchedulerSession,SCHEDULER_SESSION_COOKIE } from "@/lib/scheduler/session";
export default async function SettingsPage(){const jar=await cookies();const session=readSchedulerSession(jar.get(SCHEDULER_SESSION_COOKIE)?.value);if(!session)redirect("/scheduler/sign-in/");return <main className="mx-auto max-w-3xl px-5 py-12"><p className="text-xs uppercase tracking-[.25em] text-[#62f5e6]">Account</p><h1 className="mt-3 font-serif text-4xl">Connection controls</h1><div className="mt-9 space-y-4 rounded-3xl border border-white/10 p-6"><a href="/api/scheduler/auth/authorize/?mode=publishing&returnTo=/scheduler/settings/" className="inline-flex rounded-full bg-[#62f5e6] px-5 py-3 font-bold text-black">Connect Direct Post</a><form action="/api/scheduler/disconnect/" method="post"><button className="rounded-full border border-[#ff5269]/60 px-5 py-3 text-[#ff8b9a]">Disconnect TikTok publishing</button></form><form action="/api/scheduler/auth/sign-out/" method="post"><button className="text-sm text-white/55 underline">Sign out of scheduler</button></form></div></main>}
