# Action Checklist

## First 10 SEO fixes to implement
1. [HIGH] Some pages have weak or missing title tags - Tighten page titles so each one matches the primary intent and stays within a readable SERP length.
2. [MEDIUM] Some pages have weak or missing meta descriptions - Write page-specific descriptions that reflect the visible content, outcome, and target user intent.

## Validation steps
1. Re-run `npm run seo:audit:webgrowth`.
2. Re-run `npm run lint`.
3. Re-run `npx tsc --noEmit`.
4. Re-run `npm run build`.
